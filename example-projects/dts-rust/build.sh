


# cargo build 普通编译
# cargo build --release # 这个属于优化编译 ps: ./target/debug/hellorust.exe
# ./target/release/hellorust.exe # 如果前面是优化编译，则这样运行 ps: cargo run # 编译和运行合在一起
# cargo run --release # 同上，区别是是优化编译的